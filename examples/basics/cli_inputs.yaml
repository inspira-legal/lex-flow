workflows:
  - name: main
    interface:
      inputs: ["name", "age"]
      outputs: []
    variables:
      name: "Guest"
      age: 0
    nodes:
      start:
        opcode: workflow_start
        next: print_greeting

      print_greeting:
        opcode: io_print
        next: print_age_msg
        args:
          - node: greeting_text

      greeting_text:
        opcode: operator_add
        isReporter: true
        kwargs:
          left:
            literal: "Hello, "
          right:
            variable: name

      print_age_msg:
        opcode: io_print
        next: null
        args:
          - node: age_text

      age_text:
        opcode: operator_add
        isReporter: true
        kwargs:
          left:
            literal: "! You are "
          right:
            node: age_str

      age_str:
        opcode: operator_add
        isReporter: true
        kwargs:
          left:
            node: age_as_str
          right:
            literal: " years old.\n"

      age_as_str:
        opcode: str
        isReporter: true
        kwargs:
          value:
            variable: age
